class Rectangle:

    def __init__(self, width, height,
                 color='black', emphasis=None, highlight=0):
     pass

def moltiplicazione(a,b):
        molt = a*b
        return(molt)


a = float(input('inserisci il primo numero = '))
b = float(input('Inserisic il secondo numero = '))
molt = (moltiplicazione(a,b))  # richiamo della fuzione
print(molt)